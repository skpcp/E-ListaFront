<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bidzis.e_lista">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".User.UserAreaActivity" />
        <activity android:name=".RegisterActivity" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".User.UsersMenagmentActivity" />
        <activity
            android:name=".User.UserSiteActivity"
            android:label="@string/title_activity_user_site"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".User.ActivateNewUserActivity" />
        <activity android:name=".User.RegisterUserActivity" />
        <activity android:name=".User.DeactivateUserActivity" />
        <activity android:name=".User.ActivateUserActivity" />
        <activity android:name=".User.ShowAllUsersActivity" />
        <activity android:name=".User.ChangePasswordActivity"></activity>
    </application>

</manifest>